export const MIN_DISTANCE_TO_POST =
  Number(process.env.MIN_DISTANCE_SAME_USER) || 1000; // !KM
export const maxDistance_TO_SEE = Number(process.env.maxDistance_NEAR) || 5000; //10 KM
export const SNAP_DISAPPEAR_TIME = Number(process.env.maxDistance_NEAR) || 1; //1 Day
